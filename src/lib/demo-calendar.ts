/**
 * Demo uses an .ics file from /public. Replace `public/demo.ics` with your own,
 * or change this path (e.g. "/my-hackathon-calendar.ics").
 */
export const DEMO_ICS_PUBLIC_PATH = "/demo.ics";

/** Shown in the app as the calendar name for the demo import. */
export const DEMO_CALENDAR_DISPLAY_NAME = "Demo Calendar";
